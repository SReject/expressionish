const enum TokenType {
    UNKNOWN,
    EXPRESSION,
    LIST,
    IFSTATEMENT,
    FUNCTIONAL,
    TEXT,
    ARGUMENT,
    OPERATOR,
    LOGICAL,
    COMPARISON,
    EMPTY
}

export default TokenType;