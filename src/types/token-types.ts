const enum TokenType {
    UNKNOWN,
    EXPRESSION,
    TOKENLIST,
    IFSTATEMENT,
    FUNCTIONAL,
    TEXT,
    ARGUMENT,
    OPERATOR,
    LOGICAL,
    COMPARISON,
    EMPTY
}

export default TokenType;