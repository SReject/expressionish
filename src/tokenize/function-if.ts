import ParserOptions from '../types/options';
import { TokenizeState } from './tokenize';
export default (
    options: ParserOptions,
    meta: any,
    state: TokenizeState
) : boolean => {
    return false;
}